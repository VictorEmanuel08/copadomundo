# Copie este arquivo para functions/.env
# cp functions/.env.example functions/.env

# ── Football Data API ──────────────────────────────────────────────────
# Mesma chave usada no frontend — 10 calls/min no plano gratuito
# A Cloud Function consome 3 calls/min (matches + standings + bracket)
FOOTBALL_DATA_TOKEN=
